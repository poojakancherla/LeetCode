# Given a string s, find the longest palindromic substring in s

def palindrome(s):
    if s[0:]==s[::-1]:
            record.append((len(sub),sub))
        else:
            sub=''
            sub+=i

s=input()

        record.append((len(sub),sub))
# print(max(record)[1])
print(record)
